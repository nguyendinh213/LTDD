<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- TextView hiển thị số ngẫu nhiên -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Random Number: 0"
        android:textSize="18sp"
        android:layout_centerHorizontal="true" />

    <!-- EditText để nhập dữ liệu -->
    <EditText
        android:id="@+id/editText"
        android:layout_width="367dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/textView"
        android:layout_marginTop="10dp"
        android:hint="Nhập dữ liệu" />

    <!-- AutoCompleteTextView với gợi ý -->
    <AutoCompleteTextView
        android:id="@+id/autoCompleteTextView"
        android:layout_width="362dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/editText"
        android:layout_marginTop="10dp"
        android:hint="Tìm kiếm" />

    <!-- ImageView -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher"
        android:layout_below="@id/autoCompleteTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp" />

    <!-- CheckBox để thay đổi màu nền -->
    <CheckBox
        android:id="@+id/checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Đổi màu nền"
        android:layout_below="@id/imageView"
        android:layout_marginTop="10dp" />

    <!-- Switch bật/tắt WiFi -->
    <Switch
        android:id="@+id/switchWiFi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="WiFi"
        android:layout_below="@id/checkBox"
        android:layout_marginTop="10dp" />

    <!-- RadioGroup với các tùy chọn -->
    <RadioGroup
        android:id="@+id/radioGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/switchWiFi"
        android:layout_marginTop="10dp">

        <RadioButton
            android:id="@+id/radioButton1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lựa chọn 1" />

        <RadioButton
            android:id="@+id/radioButton2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lựa chọn 2" />
    </RadioGroup>

    <!-- Button tạo số ngẫu nhiên -->
    <Button
        android:id="@+id/randomButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/radioGroup"
        android:layout_marginTop="69dp"
        android:text="Tạo số ngẫu nhiên" />

</RelativeLayout>
